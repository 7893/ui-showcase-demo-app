import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* 
   * Vercel Native Configuration
   * Removed output: "export" and images: { unoptimized: true }
   * to fully enable Server-Side Rendering (SSR) and Image Optimization!
   */
};

export default nextConfig;
